# Основная библиотека для Telegram Bot API (версия 3.x)
aiogram>=3.4.1

# Библиотека для асинхронных HTTP-запросов (используется aiogram internally, но полезно иметь явно)
aiohttp>=3.9.0

# Для валидации данных из Web App (проверка подписи initData)
# Это рекомендуется делать на бэкенде для безопасности
pydantic>=2.5.0

# Утилита для работы с переменными окружения (чтобы не хардкодить токен)
python-dotenv>=1.0.0

# Криптография для проверки хеша WebApp initData (опционально, но рекомендуется для продакшена)
cryptography>=41.0.0